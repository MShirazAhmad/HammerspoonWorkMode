# Assets

This folder holds lightweight visual assets for the repo:

- `icons/` for menu bar or project icons
- `screens/` for overlay backgrounds used by the live Hammerspoon config

The active red warning background is `screens/soft-red-vignette-overlay.svg`.
