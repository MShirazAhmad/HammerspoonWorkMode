# Assets

This folder holds lightweight visual assets for the repo:

- `icons/` for menu bar or project icons
- `screens/` for documentation screenshots or mock screens

The current SVGs are simple placeholders so the directories are tracked in git and easy to replace later with real exported assets.
